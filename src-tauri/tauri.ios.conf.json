{
  "build": {
    "beforeDevCommand": "yarn icons && yarn vite",
    "beforeBuildCommand": "yarn icons && yarn tsc && yarn vite build"
  },
  "bundle": {
    "iOS": {
      "minimumSystemVersion": "16.0"
    }
  }
}
